schema {
    query: Query
}

type resultPlan {
    annuityPayment: String
    mainSum: String
    percentSum: String
    paymentDate: String
}

type outType {
    errorCode: String
    status: Int
    result : [resultPlan]
}

type Query {
    getPlan(
        loanId: Int
    ) : [resultPlan]
}